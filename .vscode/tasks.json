{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build, Test, Doc",
            "type": "shell",
            "command": "nix-shell --pure --run scripts/vscode.bash",
            "problemMatcher": [ ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
